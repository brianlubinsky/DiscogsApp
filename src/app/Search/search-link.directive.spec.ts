import { SearchLinkDirective } from './search-link.directive';

describe('SearchLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchLinkDirective();
    expect(directive).toBeTruthy();
  });
});
